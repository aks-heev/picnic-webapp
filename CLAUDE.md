# The Picnic Stories — Claude Working Notes

## Session Handoff — 2026-07-03 (COMPACT: /packages redesign session — critique polish + arch hero + occasion-first/city-filter/mobile — git block handed over)

• **Session scope**: /packages redesign end-to-end, all in `app.js`/`style.css` (+ `CLAUDE.md`, `admin.html`, `scripts/prerender-venues.mjs` carried from earlier same-day work). Final single-commit git block handed to user staging exactly `CLAUDE.md admin.html app.js scripts/prerender-venues.mjs style.css` — **not confirmed run; verify against user's terminal before assuming pushed.**

• **Critique pass (design:design-critique)** → fold fix (`.pkgp-wrap` 116→88 + hero/section spacing trims), venue-step copy "Where should ${tier.name} happen?" (×2 in `renderPkgVenuePicker`), `.pkgp-sub` max-width 600px, ghost-CTA override `.pkg-card-cta.btn--venue-secondary` (tint bg, #a84e66, hover fill — scoped, global class untouched), `pkgCardTopHtml` photo-less fallback `.pkg-card-media--placeholder` (full-bleed 4/3 tinted band, big doodle icon — fixes occasion-package cards' bare-icon void).

• **Arch hero (brainstorm direction A — chosen over full-bleed banner and doodles-only)**: `.pkgp-hero--split` (copy left, 1–2 sharp arch-masked photos right, `border-radius:999px 999px 14px 14px`), photo priority `packagesHeroImageUrl` (admin → Hero Image tab → "Packages page hero" panel) → universal-tier carousel photos featured-first → none = plain centered hero auto-fallback (untested branch). 2 doodles only (`.pkgp-doodle--sun/--sprig`). Blur backdrop is DEAD — rejected on bokeh blotching; don't retry blur, the arch shows photos sharp instead.

• **Occasion-first gate**: `showTiers = !!(occasion || pkgPageState.tierKey)` in `renderPackagesPage`; `.pkgp-pick-hint` replaces divider+cards until a chip is picked; "optional" removed from step label. Exemption: `?tier=` deep links + homepage CTAs skip the gate (flagged to user, no objection yet). Prerender SEO seed still shows ungated cards pre-hydration — accepted.

• **City filter**: pills at the venue step (`renderPkgVenuePicker`, reuses homepage `.city-pill`), cities from SERVICEABLE venues only, state `pkgPageState.city` (reset in `showPackagesPage`), `pkgPageSelectCity` on window, `packages_city_selected` PostHog event. Verified live: Jaipur → 4/6 venues.

• **Mobile hero**: photo-first (`.pkgp-hero-arches{order:-1}`), arch `min(64vw,250px)×min(78vw,300px)`, sun beside arch. **NOT visually verified** — no mobile-viewport tooling; user must eyeball on phone.

• **Standing gotchas reconfirmed**: sandbox git shows 5 phantom deletions (`MY_BOOKINGS_PHONE_OTP.md`, `homepage-preview.html`, `logo2.png`, `schema.md`, `test_postcss.mjs`) + bogus diff stats — never `git add -A`, never trust bash reads of app.js/style.css; Chrome MCP screenshots freeze intermittently → open a fresh tab via `tabs_create_mcp`. **Still-open from pruned 07-01 entry**: Phase 0 flat tier-pricing for the remaining 5 cafes not done; venue-first tier step not re-verified after today's shared-CSS changes; `/packages` meta `<title>` in prerender script stale.

CONTINUE FROM: confirm the git commands were run, then close the verification gaps — real-mobile hero check, no-photo hero fallback, venue-first tier step.

## Session Handoff — 2026-07-03 (Phase 2.5 occasion packages shipped + verified; /packages redesign in progress — hero backdrop tried & reverted, chip redesign done — ALL UNCOMMITTED)

• Prior packages-first carousel work (image carousel, Phases A–D — see entries below) — **CONFIRMED PUSHED** this session, user ran the git commands handed over previously.

• **Phase 2.5 occasion packages SHIPPED + verified**: migration `add_occasion_to_packages_and_seed_date_movie_night` APPLIED LIVE — `packages.occasion text` column + 4 new rows (`date_night_classic` ₹9,900 → `date_night_deluxe` ₹12,900; `movie_night_classic` ₹12,900 → `movie_night_deluxe` ₹16,900). Scope decided with user: **only Date Night + Movie Night** get dedicated packages (replace the universal ladder when selected) — Proposal/Birthday/Anniversary keep Setting/Moment/Story unchanged. Movie Screening add-on's `requires_confirmation=true` left untouched in DB (ops handles it manually per user's call — no code-level bypass).

• Supporting code (app.js): `visiblePackagesFor()` replace-semantics, `packageServiceableAt()` required-addon gating (confirmed hiding Movie Night at venue 21 "Once Upon A Time," which lacks addon 29 Movie Screening — verified via admin gap-flag row + live venue picker), generalized ladder-copy chaining (works within any occasion group, not just universal), admin `renderPackagesManager` gained an occasion `<select>` + "Not offerable here" gap-flag wording (`savePackageCard` persists it). All verified live via Chrome MCP: `/packages?occasion=...`, venue-first flow at Beige, homepage teaser (correctly unaffected — still universal-only), admin save round-trip (SQL-confirmed fresh `updated_at`).

• `/packages` visual pass (triggered by a `design:design-critique` skill run): fixed the 2-card grid gap — `.pkg-cards--page` and `.vd-layout--pkg-active .pkg-cards` changed from `repeat(3,1fr)` to `repeat(auto-fit, minmax(260px, calc((100% - 44px)/3)))` + `justify-content:center` (3-card universal layout stays pixel-identical at 332px cards; 2-card occasion views — Date Night/Movie Night — now center as a pair instead of leaving a phantom empty slot) — DONE, verified on both `/packages` and the venue-first tier step. Hero backdrop (blurred photo bled behind hero text, from a `product-management:brainstorm` session) **TRIED THEN REVERTED**: built full mechanism (`site_settings.packages_hero_image_url` key, migration `add_packages_hero_image_setting` APPLIED LIVE, new "Packages page hero" upload panel in admin's Hero Image tab, `handlePackagesHeroImageUpload`) — pipeline confirmed working (DB value correct, image fetched 200 OK, right URL applied to DOM) — but the specific photo the user uploaded has scattered string-light bokeh that blurs into uneven blotches at ANY radius (tested 48px/opacity .5, 22px/.7, 10px/.85 — all rejected live by the user, last one explicitly "doesn't look good"). Root cause is the photo, not the settings. `heroBackdropUrl` is hardcoded to `null` in `renderPackagesPage()` right now (reverted to the plain gradient hero) — the admin upload mechanism is left intact but unused, pending a photo with solid/soft tones instead of point highlights.

• Occasion chips redesigned per explicit user direction ("circles with doodles, labeled as packages"): circular `OCCASION_ICON_SVG` badges (9 hand-drawn-style stroke SVGs — cake, interlocked rings, ring+diamond, moon+sparkle, clapperboard, bouquet, bottle, grad cap, sparkles — same stroke language as the existing `PKG_TIER_ICON_SVG`) + "`<Occasion>` Packages" labels + `OCCASION_CHIP_TINTS` (4 rotating boho-palette bg/fg pairs, cycled by index), replacing the old plain-emoji pill row (`OCCASION_EMOJI` is now dead code, left in place, harmless). `.pkgp-chips` changed from `flex-wrap` to `overflow-x:auto` + scroll-snap — fixes the old problem where 9 pills wrapped to 3 rows on mobile before any package was visible. DONE, verified live (all 9 icons render correctly, active state = ring + shadow + bold label, row is single-line even at 1280px wide with room to spare).

• **RESOLVED (2026-07-03)**: "Just Because Packages" label — user chose to **remove the occasion chip entirely** from the /packages row (`OCCASIONS.filter(o => o !== 'Just Because')` at the chip render site, code comment updated). 'Just Because' stays in `OCCASIONS` itself — booking dropdowns, admin selects, and the edge-fn email subject mapping untouched. Chips toggle off on re-click, so the no-occasion → universal-ladder state stays reachable without it. Verified live: 8 chips render with correct labels; 'Just Because' was last in the array, so the other chips' tint indices didn't shift. Two harmless leftovers, flagged to user: `?occasion=Just+Because` deep links still validate (show universal ladder, no active chip), and an admin-assigned 'Just Because' package would have no chip to surface it.

• **Second `design:design-critique` pass on /packages (2026-07-03, later session — polish within current structure per user's explicit scoping, chips untouched per user's call)**, all verified live via Chrome MCP: (1) 🔴 fold fix — first package card + price were fully below a ~800px laptop fold; trimmed `.pkgp-wrap` padding-top 116→88 (mobile 96→80), `.pkgp-hero` padding 32→20 top, `.pkgp-sub` margin-bottom 36→28, `.pkgp-occasions` 32→24, `.pkgp-divider` 36→28 — card tops + MOST PICKED badge now break the fold. (2) 🔴 copy bug — "Where would you like your **The Moment**?" (double article) → "Where should ${tier.name} happen?" at both `renderPkgVenuePicker` sites (normal + zero-serviceable branch). (3) `.pkgp-sub` had no max-width (~1180px line length) → capped 600px, centered, line-height 1.6. (4) Ghost card CTAs (pink-on-white ~3.8:1, read as disabled next to the filled featured CTA — user had flagged this weakness before): new `.pkg-card-cta.btn--venue-secondary` override (tint bg, 0.5-alpha border, darker #a84e66 text, full pink fill on hover) — deliberately scoped so global `.btn--venue-secondary` elsewhere is untouched. (5) Photo-less cards (the Date Night/Movie Night occasion packages) rendered a bare 44px corner icon over a white void, silhouette-mismatched vs photo cards → `pkgCardTopHtml` fallback now renders `.pkg-card-media--placeholder`: same full-bleed 4/3 geometry as the carousel, boho radial tint, tier doodle icon centered large in a soft white circle (CSS overrides the SVGs' width/height="22" attrs to 34px). (6) `.pkgp-eyebrow` 11→12px. Verified: /packages fold + CTAs + Date Night placeholder bands + venue-step copy ("Where should The Setting happen?", 6 venue cards w/ QA override), homepage teaser unaffected (3 photo cards render as before). NOT re-verified: venue-first tier step (shares the same classes; low risk) and real-mobile viewport (standing Phase D gap).

• **Arch hero SHIPPED to /packages (2026-07-03, after a `product-management:brainstorm` run — user picked "direction A" over full-bleed banner and doodles-only)**: `.pkgp-hero--split` grid (copy left-aligned left, photos right) with 1–2 SHARP photos in tall boho arch masks (`.pkgp-arch`, `border-radius:999px 999px 14px 14px`, white ring border + soft shadow) — the deliberate opposite of the reverted blur-backdrop (blur hid the photos and broke on bokeh; sharp arch crops sidestep that failure mode entirely). Photo priority in `renderPackagesPage()`: `packagesHeroImageUrl` (site_settings — the backdrop attempt's admin-upload mechanism, now actually consumed) → universal tiers' carousel photos featured-first (`tierHeroPhotos`) → zero photos = `--split` class not applied, plain centered hero returns automatically. Side arch only renders given a 2nd distinct photo (Set-deduped). Plus 2 hand-drawn doodles (`.pkgp-doodle--sun` pink top, `--sprig` sage bottom-left; same stroke language as chips, opacity .4, aria-hidden, capped at two deliberately). ≤860px: single centered column, side arch + sprig hidden, main arch 190×225. Verified live at 1512px: arches render with real photos (main arch was showing the previously-uploaded site_settings hero photo — it works fine sharp), copy left-aligned, doodles in place, chips/divider/cards below all unaffected. **Known trade-off, accepted in brainstorm**: hero is now ~150px taller, so tier cards slipped back below the ~800px fold (chips still visible) — partially undoes the earlier fold fix in exchange for the hero actually carrying the brand. NOT verified: no-photo fallback branch (would need site_settings cleared + all tier images emptied), real mobile.

• **Occasion-first + city filter + mobile arch rework (2026-07-03, user's 4-point feedback batch)**, all verified live via Chrome MCP JS-state checks (screenshot tool flaky again — same standing issue): (1) **Occasion gate**: `/packages` no longer shows the tier ladder until an occasion chip is picked — `showTiers = !!(occasion || pkgPageState.tierKey)` in `renderPackagesPage`; hint state `.pkgp-pick-hint` ("Pick an occasion above…") replaces divider+cards; "optional" removed from the step label. **Deliberate exemption, flagged to user**: explicit tier (deep link `?tier=`, homepage teaser CTAs) skips the gate — visitor already chose a package (verified: `?tier=story` renders 3 cards + venue picker, fresh `/packages` renders hint only). Note: prerendered SEO seed (`buildPackagesPage`) still shows tier cards pre-hydration — crawlers see them, users get the gated state after hydration; acceptable, not changed. (2) **City filter**: pills ("All cities" + 📍 per city) above the venue grid in `renderPkgVenuePicker` — venue step chosen deliberately (occasion/package are city-agnostic; venue is the first city-bound decision). Reuses homepage `.city-pill` styling; cities derived from SERVICEABLE venues only; invalid remembered city falls back to all; `pkgPageState.city` (reset on `showPackagesPage`), new `pkgPageSelectCity` on window + `packages_city_selected` PostHog event. Verified: 4 pills, Jaipur → 4 of 6 venues, all `· Jaipur`. (3) **Mobile arch**: ≤860px hero is now photo-first (`.pkgp-hero-arches { order:-1 }`), arch enlarged to `min(64vw,250px)×min(78vw,300px)`, sun doodle repositioned beside the arch top-right via `calc(50% - min(32vw,125px) - 52px)` — replaces the text-then-small-arch stack the user flagged as odd. NOT visually verified (standing no-mobile-viewport tooling gap — CSS-only reasoning, user should eyeball on a phone). (4) Told user how arch photos are managed: admin → Hero Image tab → "Packages page hero" panel (site_settings, main arch priority) → tier carousel photos featured-first fill remaining arch.

• **UNCOMMITTED** this session, on top of the already-pushed carousel work: `app.js` (occasion-packages logic + hero subhead copy + grid fix + hero-backdrop feature added-then-disabled + chip redesign + critique-pass fixes: venue-step copy ×2 + `pkgCardTopHtml` placeholder fallback + arch-hero markup/photo-priority logic in `renderPackagesPage` + occasion-gate/`showTiers` + city-filter pills/`pkgPageSelectCity`/`pkgPageState.city` — this is a LOT in one file, consider whether to split into multiple commits), `style.css` (grid fix + hero-backdrop CSS, still present but unused + chip CSS rewrite + critique-pass CSS: spacing trims, sub max-width, pkg-card-cta override, placeholder-media block + arch-hero block: `.pkgp-hero--split`/`.pkgp-arch*`/`.pkgp-doodle*` + `.pkgp-pick-hint`/`.pkgp-city-filter` + mobile photo-first hero rework — old `.pkgp-hero-backdrop`/`-scrim` rules now dead code, left in place), `admin.html` (new "Packages page hero" panel in the Hero Image tab), `scripts/prerender-venues.mjs` (hero subhead copy synced to match app.js — but its meta `<title>` for `/packages` is now stale, still reads "Setting, Moment & Story," doesn't mention the new occasion packages; not fixed, user hasn't asked). Standing gotchas reconfirmed this session: sandbox bash `git status`/`git diff` still shows phantom deletions of 5 untouched files (`MY_BOOKINGS_PHONE_OTP.md`, `homepage-preview.html`, `logo2.png`, `schema.md`, `test_postcss.mjs`) — exclude from any `git add` line, never trust bash-computed repo state here; Chrome MCP `Page.captureScreenshot` is flaky/times out intermittently this session — workaround is opening a fresh tab via `tabs_create_mcp` rather than retrying the same stuck tab.

## Session Handoff — 2026-07-03 (Phase 2 packages-first: per-package image carousel — full pipeline built + verified on all 3 surfaces — Phases A+B+C+D+polish+carousel UNCOMMITTED)

• **Out-of-plan feature, user-initiated**: "adding per package images in the package card... carousel at the top of the package card." Clarified scope upfront via AskUserQuestion — user chose: carousel on **all three** `.pkg-card` surfaces (/packages, homepage teaser, venue-first tier step), **full admin upload pipeline now** (not display-only), **dots + swipe/arrows** nav style (not auto-advance).

• **DB migration `add_images_to_packages_and_bucket` APPLIED LIVE**: `packages.images jsonb NOT NULL DEFAULT '[]'` + new `package-images` Storage bucket (public read, authenticated insert/delete) — same RLS pattern as the existing `venue-images`/`addon-images`/`site-images` buckets. `get_advisors` run post-migration: only the expected new-bucket-policy note, no new risk class.

• **Admin UI (app.js, `renderPackagesManager`)**: each tier card gained a "Carousel images" section (`renderPkgAdmImages`/`readPkgAdmImages`/`handlePkgAdmImageUpload`/`addPkgAdmImagesMulti`/`removePkgAdmImage`) — near-exact port of the existing venue-images upload pattern, parameterized by `pkgId` since all 3 package cards render inline simultaneously (not one-modal-at-a-time like the venue form). `savePackageCard` now reads the images list and writes it to `packages.images` alongside the existing name/tagline/add-ons fields.

• **Shared carousel component (app.js, after `pkgTierIconHtml`)**: `pkgCardMediaHtml(images, name)` renders the image track + dots (or falls back to the existing tier icon when a package has zero images — verified both states render correctly side-by-side); `pkgCardTopHtml(tier, key)` wraps it; `pkgCarouselGoTo`/`pkgCarouselNav` handle index changes; `setupPkgCarouselDelegation()` registers ONE document-level delegated click listener at bootstrap (not per-card listeners) since cards are torn down/rebuilt on every re-render. All three render sites (`renderPackagesPage`, `renderHomePackagesSection`, `showPackageStep`) now call `pkgCardTopHtml()` instead of the old bare `pkgTierIconHtml()` call — single shared helper, so the carousel can't drift between surfaces the same way pricing/icons are already guarded against drifting.

• **CSS** (style.css, after the icon/gradient polish-pass block): `.pkg-card-media` (negative-margin bleed to the card's edges, `aspect-ratio:4/3`, `border-radius:15px 15px 0 0` — matches the corner-radius-clamping lesson from the accent-bar bug fixed earlier this session, confirmed NOT to reintroduce that issue since this radius is on a box tall enough to render it correctly), `.pkg-card--featured .pkg-card-media` (adjusted margin for the featured card's extra border), `.pkg-card-media-track`/`-arrow` (hover-only visible per the user's chosen nav style)/`-dots`/`-dot`/`--active`, bumped `.pkg-card-badge` to `z-index:3` so the "MOST PICKED" badge stays above the new image layer.

• **Verified live end-to-end via Chrome MCP against real Supabase data** (temporary SQL seed of 3 Unsplash URLs into `packages.images` for the 'moment' tier — chosen because both attempts to push a real file through the admin upload button failed on tooling limits: `upload_image` with a screenshot `imageId` errored "unable to access message history," and `save_to_disk` on screenshots is a no-op in this environment — so the upload button itself is unverified this session, though it's a near-1:1 port of the already-proven venue-image upload code path):
  - **/packages**: carousel renders with 3 dots + image for "The Moment"; "The Setting"/"The Story" (no images) correctly fall back to their tier icons. Clicked the 3rd dot → carousel advanced to that image, confirming `pkgCarouselGoTo` click delegation works.
  - **Homepage `#packages-section`**: same carousel, same dots, same fallback behavior — confirms the shared-helper approach picked it up automatically with zero surface-specific code.
  - **Venue-first tier step** (`beige-cafe?packages=1`, walked through date → slot → guests → Book Now to reach the real `showPackageStep()` render): same carousel again, "MOST PICKED" badge unaffected by the new image layer's z-index.

• **Test data reverted**: `UPDATE packages SET images = '[]'::jsonb WHERE key = 'moment'` run after verification — confirmed via SELECT all 3 tiers are back to `images: []`, matching their pre-session state (no package had real images before this feature).

• **Still unverified**: the actual admin upload button's file-picker → Storage → URL round trip (blocked by the tooling limits above — needs a real browser file upload test, e.g. by the user directly in admin, or a future session with working `file_upload`/`upload_image`). Swipe/touch-drag gesture also untested (only click-based dot/arrow nav exercised) — should hold up since it's the same underlying `pkgCarouselNav(delta)` a swipe handler would call, but not confirmed on an actual touch device.

• **UNCOMMITTED**: app.js + style.css carry this on top of everything below. DB migration + bucket + RLS are live (schema changes aren't the "uncommitted code" rule — those are already applied). No commit run — per standing rule, do not commit without explicit go-ahead.

## Session Handoff — 2026-07-03 (Phase 2 packages-first: visual polish pass on /packages + shared pkg-card — Phases A+B+C+D+polish UNCOMMITTED)

• **Out-of-plan visual pass**, triggered by user feedback that /packages "looks stale and boring" (screenshot attached). Ran the `design:design-critique` skill framework informally (no Figma/design tool connected, so critique was screenshot-based) then implemented directly rather than writing a separate critique doc — findings: flat cream background with no depth, plain text-only tier cards with no iconography, plain pill chips, no visual separation between the occasion step and the tier step.

• **New shared helpers (app.js, right after `OCCASIONS`)**: `OCCASION_EMOJI` (one emoji per occasion, missing keys just render nothing) and `PKG_TIER_ICON_SVG`/`pkgTierIconHtml(key)` (small line icons matching the existing `.service-icon-wrap` style; unknown/future tier keys — Phase 2.5 occasion packages, or an admin-added tier — fall back to a sparkle icon rather than rendering blank). Deliberately built as **one shared helper used in all three places `.pkg-card` is rendered** — `renderPackagesPage()` (/packages), `renderHomePackagesSection()` (homepage), and `showPackageStep()` (venue-first flow) — so the tier cards can't visually drift between surfaces the way pricing logic is already guarded against drifting.

• **CSS additions (style.css)**: `.pkgp-wrap` gained a two-corner radial-gradient wash (rose top-left, sage top-right — decorative only, no new images); `.pkgp-hero`/`.pkgp-eyebrow` (new "Curated Picnic Experiences" label above the H1, mirrors `.hero-eyebrow`/`.phs-eyebrow`); `.pkgp-divider` (flexbox gradient-line + ✦ glyph between the occasion chips and the tier cards — built as 3 flex children, not an absolutely-positioned "erase the line" trick, so it doesn't fight the wrap's new gradient background); `.pkg-card-icon` (44px tinted rounded-square icon slot, pink tint bumped up on `.pkg-card--featured`); `.pkg-card::before`/`.pkg-card--featured::before` (4px top accent bar, sage default / pink gradient on featured — confirmed doesn't clip the `.pkg-card-badge` since that badge sits at `top:-11px`, outside the card's own box, and `.pkg-card` intentionally has no `overflow:hidden`); `.pkgp-chip-emoji` + a box-shadow on `.pkgp-chip--active` for more pop.

• **Verified live via Chrome MCP screenshots** (this session's `resize_window` mobile-viewport issue didn't block this — checked at normal desktop width) on both `/packages` (eyebrow/wash/divider/icons/accent-bars all render; scrolled through all 3 cards, CTAs and ladder-copy inclusion lists unaffected) and the homepage `#packages-section` (same icons/accent-bars inherited automatically since it reuses `.pkg-card`, confirming the shared-helper approach worked — no visual drift between the two surfaces).

• **Not touched**: the prerendered `/packages` SEO seed content in `scripts/prerender-venues.mjs` (`buildPackagesPage()`'s static `.pr-pkg-card` markup) still has the old plain styling — acceptable since it's only visible for a moment pre-hydration and gets fully replaced by `renderPackagesPage()`'s live markup; not worth duplicating the new visual treatment into throwaway pre-JS content. Also didn't touch the venue-first `showPackageStep()` layout/CSS beyond the shared `.pkg-card` treatment it already inherits.

• **Bug fix same session**: user flagged the `.pkg-card` top accent bar had a square, un-rounded corner overhanging the card's own rounded edge (screenshot). Root cause: the bar was a `::before` pseudo-element (`top:0;left:0;right:0;height:4px`) with its own `border-radius:15px 15px 0 0` — a 4px-tall box can't actually render a 15px corner radius (CSS clamps/scales it down since it exceeds the box height), so the bar's flat bottom corners sat outside the card's real 16px curve at that height, poking past it. **Fix**: removed both `::before` rules entirely; the accent bar is now painted as a second `background` layer on `.pkg-card`/`.pkg-card--featured` itself (`linear-gradient(...) top left / 100% 4px no-repeat, var(--boho-white)`) — backgrounds are clipped to the border-box's actual `border-radius` automatically, so it follows the real curve with no separate-geometry math needed. General lesson: don't fake a decorative edge-bar with a short separate pseudo-element that has its own radius — paint it as a background layer on the already-rounded parent instead.

• 🔴 **Bash-mount staleness hit `style.css` this session, worth flagging since it triggered a false alarm**: a brace-balance check via sandbox bash (`python3` reading the file at the `/sessions/.../mnt/picnic-webapp` path) reported the file ending mid-declaration at line 9430/9431 with an unclosed brace — looked like real corruption from the edit. Re-checked via the Read tool (not bash) and the file was intact well past line 9500 with everything properly closed; confirmed via a live browser load that all downstream CSS (venue picker, homepage section, chip/divider styles) still rendered correctly. Same class of issue as the recurring `git status`/`git diff`/`wc -l` staleness documented in earlier entries below — now confirmed to also hit plain `cat`/`wc -l`/Python file reads on style.css, not just git plumbing. **Rule reinforced: never trust a bash-computed view of app.js/style.css in this sandbox — for anything (line counts, brace balance, git state), verify via the Read tool or a live browser load instead.**

• **UNCOMMITTED**: app.js + style.css carry this on top of everything in the Phase D entry below. No commit run — per standing rule, do not commit without explicit go-ahead.

## Session Handoff — 2026-07-03 (Phase 2 packages-first: Phase D — Meta Pixel + analytics verified; mobile pass INCOMPLETE — Phases A+B+C+D UNCOMMITTED)

• **Phase D scope per the plan doc**: sessionStorage hardening, zero-venue fallback, Meta Pixel ViewContent, mobile pass. First two were **already done in earlier phases** — re-verified by re-reading the code, no new work needed: sessionStorage mirror/restore lives in `pkgPageSelectVenue()` + the bootstrap `ps_pending_pkg` restore (Phase A); zero-venue fallback is the existing `revealPackagesEntryPoints()` gate (hides hero CTA + homepage section) plus `renderPackagesPage()`'s own "coming soon" branch (Phase A/C). Only ViewContent + mobile were net-new this session.

• **Meta Pixel ViewContent — DONE**: added to `showPackagesPage()` (app.js, right after the existing `track('packages_page_viewed', ...)` call), guarded by `typeof fbq === 'function' && venues.length`. Fires `content_ids` = visible universal tier keys, `content_type: 'product_group'`, `value` = min tier price across enabled venues (same `packageTierPriceAt`/`PKG_PAGE_BASE_ADULTS` computation used elsewhere — no new pricing logic). Fires exactly once per page view (inside `showPackagesPage`, not `renderPackagesPage`, so occasion/tier re-renders on the page don't re-fire it) — mirrors `setupVenueCardViewContentObserver`'s event shape.

• **Verified live WITHOUT touching the real Meta Pixel dataset**: monkey-patched `window.fbq` to a recorder in the browser console (Chrome MCP `javascript_tool`), called `window.showPackagesPage(true, {tierKey:'moment'})` directly, captured the exact call args, restored the original `fbq` after — confirmed `content_ids:['setting','moment','story']`, `value:7900` (matches Beige's current base price). This was deliberate: the real Meta Pixel script IS loaded on `localhost:5173` (`typeof window.fbq === 'function'`), so a normal click-through would have sent real test traffic into the production ad account's pixel — the monkey-patch avoided that.

• **PostHog events confirmed landing in the real project** (queried via the PostHog MCP, `execute-sql` against `events` for `event LIKE 'packages_%'` in the last 6h): `packages_page_viewed` — 2 rows, most recent `2026-07-03T08:30:01+05:30`. 🔴 **Side effect to know about**: this session's Phase B/D browser testing sent 2 real `packages_page_viewed` events into the live PostHog project (id 482400) — harmless (low volume, clearly dev-origin), but flagging since it's a real consequence of testing against `localhost` with a real `VITE_POSTHOG_KEY` in `.env.local`. `packages_tier_selected`/`packages_occasion_selected`/`packages_venue_selected` did NOT show up — not a bug, just untested this session: those only fire from `pkgPageSelectTier()`/`pkgPageSelectOccasion()`/`pkgPageSelectVenue()`, i.e. a user clicking within the already-rendered page, not from passing `tierKey` via `showPackagesPage()`'s deep-link `opts` (which is what the monkey-patch test and the homepage CTA both do).

• 🔴 **Mobile pass NOT actually done — tooling limitation, not skipped by choice**: Chrome MCP's `resize_window` reported success at 390×844 but `window.innerWidth`/`outerWidth` stayed at 1280 across three attempts (fresh tab each time) — the real OS Chrome window did not visually resize (likely maximized/snapped and the extension can't override that in this environment). **No CDP device-emulation tool was available to force a true mobile viewport.** Did a static CSS audit instead as a substitute (NOT equivalent to a real rendered check): homepage section reuses `.pkg-cards`/`.pkg-card` (already collapses to 1 column below 769px, no fixed widths) and `.container` (fluid padding, shared with every neighboring section); `/packages` reuses `.venue-grid` (audited breakpoints at 900px/580px) and already had a `@media(max-width:720px)` padding rule from Phase A. New CSS this session (`.phs-*`, `.pkg-card-occasions`) uses only relative units / `clamp()`. **This is reasoning from the stylesheet, not a verified screenshot — still do a real phone/DevTools-emulation check before calling Phase D done.**

• **UNCOMMITTED**: app.js now also carries the ViewContent block on top of Phases A+B+C. No commit run — per standing rule, do not commit without explicit go-ahead.

• **REMAINING for Phase D**: real mobile viewport verification (use actual DevTools device toolbar or a phone, not this session's tooling), then Phase 2 as a whole is feature-complete pending `npm run build && npm run preview` on Windows (still not run for real this whole project — every verification across Phases A–D has been either live-`localhost:5173` browser checks or logic/mock harnesses, never a real prerendered-`dist/` build).

## Session Handoff — 2026-07-03 (Phase 2 packages-first: Phase C prerender/SEO built & logic-tested — Phases A+B+C UNCOMMITTED)

• **Phase C done**: `scripts/prerender-venues.mjs` gained `buildPackagesPage(supabase, template)`, called from `main()` right after `buildCityPages()`, writing `dist/packages.html` (served at `/packages` via Vercel's `cleanUrls` — no `vercel.json` change needed, same as venue pages) and pushing its URL into the sitemap array (15→16 URLs once run). Follows the exact `buildPage()` pattern (SPA-shell clone, not the bespoke city-page HTML): swaps `<title>`/meta/OG/Twitter/canonical/JSON-LD in the compiled `dist/index.html`, flips `#home-page` → inactive and `#packages-page` → active, injects a crawlable SEO block into `#packages-content` (tier name/tagline/From-price/inclusion list per tier + "Available at: <linked venues>"), and appends the same `#pr-loader` overlay — but its `MutationObserver` watches for `.pkgp-wrap` (real hydrated markup) instead of `[class*="vd-"]`, since the static seed block deliberately uses non-colliding `.pr-pkg-*` classes so the loader can't mistake placeholder content for hydration.

• **JSON-LD**: `OfferCatalog` with one `Offer`/`itemOffered:Service` per tier, `price` = build-time "From ₹X", `url` = `/packages?tier=<key>`.

• **Price math ported, not imported**: `getVenuePriceAt()`/`tierPriceAtVenue()` in the script are a manual duplicate of `getVenuePrice()`/`packageTierPriceAt()` from app.js — app.js can't be imported into this Node script (it depends on `import.meta.env` + DOM). **Keep both in sync by hand if the pricing model changes.** Only queries `packages_enabled=true` cafe venues (mirrors `pkgEnabledVenues()`); zero such venues → page still builds with a "coming soon, browse venues" fallback instead of card content (mirrors the client's own zero-venue fallback), never skipped entirely. Missing `packages`/`venues`/`add_ons` fetch → page is skipped (warns, doesn't crash the whole prerender build) since a broken /packages page is worse than a missing one.

• **"Client price rehydration on load" needed no new code** — it's already inherent: the existing `packagesPath` bootstrap branch (app.js ~L8232) calls `showPackagesPage(false, {...})` unconditionally on `/packages`, which does `el.innerHTML = ...` inside `renderPackagesPage()`, wholesale-replacing the prerendered SEO placeholder with live-priced markup the moment app.js hydrates — same mechanism the venue pages already rely on. Nothing venue-page-specific had to be duplicated for this to work.

• **Verified via a throwaway Node harness, NOT a browser** (`npm run build` needs real Supabase creds + isn't reliable in this sandbox per the standing esbuild/mount warning below): stripped `main()`'s auto-invoke from a copy of the script, fed `buildPackagesPage()` a mocked Supabase client (3 tiers, 2 packages-enabled venues, one venue missing add-on 19 to exercise the existing silent-drop behavior) and a minimal fake `index.html` template. All 12 assertions passed: title/canonical/OG/Twitter swapped, `#home-page` deactivated, `#packages-page` activated, SEO block + tier cards + venue links injected, loader present and watching `.pkgp-wrap`, JSON-LD present and valid JSON. Price math checked out: Setting From ₹7,900 (min of 7900/13900, no add-ons), Moment From ₹10,700 (7900+2800 addon sum beats 13900+2800). **This validates the function's logic, not a real end-to-end build** — still run `npm run build` for real (with live env vars) before deploying, on Windows per the standing sandbox-build caveat.

• 🔴 **New bash-mount gotcha this session**: temp test files written directly into the mounted `D:\Projects\picnic-webapp` (should have used the scratch outputs dir instead) hit `rm: Operation not permitted` — the platform blocks deletes in this connected folder until explicitly allowed via `allow_cowork_file_delete`. Recovered fine once granted. **Lesson: never write scratch/throwaway files directly into the user's project folder — use the outputs/scratchpad dir for anything not meant to ship.**

• **UNCOMMITTED**: `scripts/prerender-venues.mjs` now also carries Phase C on top of Phases A+B (`index.html`/`app.js`/`style.css`/`docs/PHASE2_PACKAGES_FIRST_PLAN.md`). No commit run — per standing rule, do not commit without explicit go-ahead. **`git status`/`git diff` run via sandbox bash this session showed phantom deletions of files never touched (`MY_BOOKINGS_PHONE_OTP.md`, `homepage-preview.html`, `logo2.png`, `schema.md`, `test_postcss.mjs`) — do not trust this, cross-check against the user's own terminal before drawing any conclusion about repo state (recurring issue, see entries below).**

• **NEXT = Phase D**: sessionStorage hardening already done in Phase A; remaining — zero-venue homepage/hero-CTA fallback already handled by the `revealPackagesEntryPoints()` gate (no extra work needed there), Meta Pixel `ViewContent` on `/packages`, mobile pass. Then: real `npm run build && npm run preview` browser check of `/packages` (dev server doesn't serve prerendered pages — known caveat, dist-only, same as city pages).

## Session Handoff — 2026-07-02 (Phase 2 packages-first: Phase B homepage section built & browser-verified — Phases A+B UNCOMMITTED)

• **Phase B done**: new `<section id="packages-section">` in index.html between `.services` and `#venues-section`, hidden (`style="display:none"`) until `revealPackagesEntryPoints()` (called from `loadVenues()`) finds ≥1 `pkgEnabledVenues()` — same gate as the hero CTA, so both entry points appear/disappear together. `renderHomePackagesSection()` (app.js, right after `revealPackagesEntryPoints`) awaits its own `loadPackages()`/`loadCatalogsForVenues()` rather than trusting call order against the concurrent bootstrap calls; builds one `.pkg-card` per `visiblePackagesFor(null)` (universal tiers only — no occasion context on the homepage teaser) reusing the exact `.pkg-cards--page`/`.pkg-card*` CSS from the /packages page, so "From ₹X" is guaranteed identical between the two surfaces (same `packageTierPriceAt(...,PKG_PAGE_BASE_ADULTS)` call). New `.pkg-card-occasions` line reverse-looks-up `OCCASION_DEFAULT_TIER` ("Perfect for Birthday, Date Night, Bridal Shower"). CTA is a real `href="/packages?tier=<key>"` (new-tab correct) with `onclick` SPA nav via `showPackagesPage(true,{tierKey})` (in-app, tier preselected, venue picker auto-shown) — mirrors the hero CTA pattern.

• **New CSS** (style.css, after the /packages page block): `.packages-home-section` (cream bg, border-top, mirrors `.venues-section` padding), `.phs-header/-eyebrow/-title/-sub` (bespoke, centered — did NOT reuse the global left-aligned `.section-label/.section-heading` to avoid overriding their alignment elsewhere), `.pkg-card-occasions`.

• **Verified live via Chrome MCP on the user's `localhost:5173`** (dev server had to be started mid-session — was down at first check): section renders 3 cards (Setting ₹7,900 / Moment ₹11,000 / Story ₹24,000 — reflects Beige's post-cut `base_price`), grid is a true 3-column `.pkg-cards--page` at 1280px, occasion lines correct per `OCCASION_DEFAULT_TIER`, all three CTA hrefs correct. Clicked the Story CTA → SPA-navigated to `/packages`, `.pkg-card--active` = "The Story", venue picker auto-rendered — deep-link parity confirmed end to end (exit criterion from the plan doc: "CTA deep-links with tier preselected" — met).

• **NEXT = Phase C**: prerender `dist/packages.html` (extend `scripts/prerender-venues.mjs`'s `buildPackagesPage()`), sitemap 15→16, meta/OG/JSON-LD `OfferCatalog`, `#pr-loader` pattern, client price rehydration on load (build-time From-prices are placeholders). Then Phase D (Meta Pixel ViewContent on /packages, sessionStorage hardening already done in Phase A, mobile pass).

• **UNCOMMITTED**: index.html, app.js, style.css now carry BOTH Phase A (prior entry below) and Phase B changes, plus `docs/PHASE2_PACKAGES_FIRST_PLAN.md`. No commit run this session — per standing rule, do not commit without explicit go-ahead.

## Session Handoff — 2026-07-02 (Phase 2 packages-first: plan doc + /packages Phase A + hero CTA built & browser-verified; sidebar bug fixed+committed — Phase A UNCOMMITTED)

• **Phase 2 plan** → `docs/PHASE2_PACKAGES_FIRST_PLAN.md` (plan-optimizer, 90/100), incl. **Phase 2.5** forward-compat for occasion-specific packages (date/movie night planned): nullable `packages.occasion` migration, augment-vs-replace decision deferred until packages defined, **required-addon rule** (silent add-on drop in `selectPackageTier` ~L2286 is fatal for themed packages — must hide package at venues that can't serve it, constrain From-price min + admin gap flags), admin add/delete UI becomes required, `OCCASIONS` key canon. Phase A + hero CTA IMPLEMENTED + verified live; **NEXT = Phase B** (homepage "Our Packages" section, 3 priced tier cards, hidden unless ≥1 enabled venue), then C (prerender `dist/packages.html` + sitemap 16 + meta/JSON-LD OfferCatalog + client price rehydration), then D (Meta Pixel ViewContent, mobile pass).

• **Handoff keystone (app.js)**: `appState.pendingPackage={occasion,tierKey,venueId}` + sessionStorage `ps_pending_pkg` (restored at bootstrap); `showVenuePage` clears it on venueId mismatch + prefills `bookingOccasion` from it; Book-Now click handler consumes it via the SAME `selectPackageTier()` the venue-first flow uses → tier step skipped, one code path, zero drift (parity verified live: identical locked bundle/prices both paths). Guest-step occasion dropdown: HIDDEN when `pending.occasion` set; shown w/ copy "Optional — helps us prep your setup" when occasion was skipped on /packages; original copy + behavior venue-first — all three branches verified live.

• **/packages page (app.js, inserted after `defaultTierForOccasion`)**: `pkgPageState`, `showPackagesPage(push,opts)` (awaits `loadPackages` via new `packagesLoaded` flag + `loadVenues`), `renderPackagesPage`, `renderPkgVenuePicker`, `pkgPageSelect{Occasion,Tier,Venue}`, `onPkgVenueGridClick` — all exposed on `window`. Deep links `?tier=&occasion=`; bootstrap `packagesPath` branch + popstate branch (`/^\/packages\/?$/`). `visiblePackagesFor(occasion)` filters `PACKAGE_TIER_ORDER` — used by BOTH /packages and `showPackageStep` (ladder copy chains only within universal packages). `packageTierPriceAt(v,tier,catalog,adults)` split from `packageTierPrice` (`PKG_PAGE_BASE_ADULTS=2` so stale `appState.adults` can't skew From-prices). `loadCatalogsForVenues(ids)` = ONE `add_ons.select('*, venue_add_ons!inner(venue_id)').in()` → `appState.venueCatalogCache` Map. `loadPackages` now carries `occasion: pkg.occasion || null` through.

• **User-directed refinements**: venue picker reuses `venueCardHtml(v,{priceHtml})` showing firm tier price — grid class must be `venue-grid` NOT `venues-grid` (that's the flex section stack; caught live as a 1-column bug); `.pkgp-venue-grid .venue-card-pkg-badge{display:none}`. Setting card lists `SHARED_SETUP_LABELS` (11 items, extracted const; `venueSetupLabels()` = shared + `metadata.includes`). Hero: **Contact Us replaced** by `#hero-packages-cta` "View Packages" (per user choice — floating WhatsApp covers contact), hidden until `revealPackagesEntryPoints()` (called from `loadVenues`) finds ≥1 `pkgEnabledVenues()`; SPA nav via inline onclick, real href for new-tab. Ghost style reads visually weak next to filled Book Now — user may want an outline variant.

• **Sticky sidebar overflow FIXED + committed by user** (style.css): `.vd-booking-card{max-height:calc(100vh - 104px);overflow-y:auto;overscroll-behavior:contain}` — cap on the CARD not `.vd-sidebar` so its box-shadow isn't clipped; reset (`max-height:none;overflow-y:visible`) in the ≤768px block + `.vd-layout--pkg-active`. Verified through calendar/slot/guest/package steps.

• **Data facts**: Beige(14) `base_price` changed 9,900→7,900 TODAY by user mid-session (Phase 0 setup-only pricing presumed; `metadata.tiers` now stale at 9,900 — dead data, flat path short-circuits it). User's browser holds sticky `ps_packages=1` QA override → all 6 cafes show in picker; production = Beige only (`packages_enabled`). Parked: venue 21 Skyshots insert (`venue_add_ons` venue 21 verified still missing addon 27; ₹4,000 elsewhere), Phase 0 pricing for remaining 5 cafes. Admin Packages tab verified working in browser (derived prices, per-venue table, venue-21 gap flag render correctly; earlier flat-pricing DB state confirmed via SQL).

• **UNCOMMITTED**: `app.js`, `index.html`, `style.css` (pkgp styles), `docs/PHASE2_PACKAGES_FIRST_PLAN.md`. Commit cmd handed over (`feat: packages-first /packages entry point (Phase 2A)…`), not confirmed run. Chrome MCP caveat: `Page.captureScreenshot` times out intermittently on this tab — verify via `javascript_tool` DOM reads instead.

## Session Handoff — 2026-07-02 (admin Packages panel built + shipped: DB migration, pricing model simplified, 3 venue-form display bugs fixed — UNCOMMITTED)

• **Goal: admin panel for package pricing + inclusions**, triggered by a base_price/`metadata.tiers` desync bug found while checking why package "From" prices looked stale. Ran plan-optimizer first (3 rounds, final score 89/100), then implemented directly — plan lives only in this conversation, not saved as a doc.

• **DB migration `add_pricing_columns_and_packages_tables` APPLIED LIVE**: `venues.free_guests_upto` (int) + promoted `overage_per_person` (was metadata-only) — populated ONLY for the 6 flattened cafe venues (14 Beige, 18 Sunroom, 19 Castle Valley, 20 Om Niwas, 21 Once Upon A Time, 24 House of Amer). `getVenuePrice()` now does flat `base_price` through `free_guests_upto` then linear overage for those; every other venue (partner_bnb 22/23/25 — genuine multi-step stay pricing; self_managed/custom/combo 5/15/16/17 — zero tiers) has `free_guests_upto=NULL` and falls through to the untouched legacy `metadata.tiers` path. **Caught by a parity SQL check before shipping**: first attempt set `free_guests_upto=capacity_max` for the zero-tier venues assuming it was lossless — wrong, it silently added overage charges past `capacity_max` that never existed before. Reverted to NULL; reran parity across guest counts 1–20 for every active venue → zero mismatches.

• **New `packages`/`package_add_ons` tables** replace the hardcoded `PACKAGE_TIERS` JS constant, seeded 1:1 from it (setting/moment/story, same add-on IDs) so nothing changed live on migration day. `PACKAGE_TIERS`/`PACKAGE_TIER_ORDER` in app.js are now `let`, overwritten by new `loadPackages()` at bootstrap (DOMContentLoaded, alongside `loadTeams()`) — hardcoded values are only the fallback if that fetch fails. RLS mirrors `admin_update_venues`; `get_advisors` shows only pre-existing warnings.

• **New admin "Packages" tab** (app.js `loadPackagesManager`/`renderPackagesManager`/`savePackageCard`): per-tier card (Setting/Moment/Story) — name/tagline/featured/add-ons checklist, price shown **read-only, always derived** (base_price + sum of checked add-on catalog prices — never typed in, specifically to not recreate the drift bug this feature started from). Per-venue price table flags add-on gaps — found venue 21 "Once Upon A Time" is missing add-on 27/Skyshots from its `venue_add_ons` catalog, so its Story tier has been silently underpriced/short one item this whole time — and shows food-included read from each venue's own `metadata.food_offline` (not editable per-package, single source of truth stays venue-level).

• **Venue form changes**: "Pricing Tiers" UI now gated `partner_bnb`-only (new `.vf-not-partner-only` class shows a plain `free_guests_upto` field for everyone else); added `#vf-food-offline` checkbox (cafe-only) wired to `metadata.food_offline` — previously only settable via direct SQL, invisible in admin.

• **Fixed 3 venue-form display bugs** (user-reported via screenshot; none were data loss — DB had correct values throughout, verified via direct query): (1) team select always reverting to the first team on reopen — `openVenueForm()` set `#vf-team`'s value in `populateVenueForm()` *before* rebuilding its `<option>` list right after, wiping the selection; reordered. (2) Packages checkbox always unchecked on reopen — `loadVenueManager()`'s explicit `.select()` was missing `packages_enabled` (and this session's own new `free_guests_upto`/`overage_per_person`); added all three. (3) No food-included indicator existed at all — added (see above).

• 🔴 **Sandbox bash mount unreliability confirmed again, now also hitting `git diff`** (not just app.js reads / `git status`, per prior entries): `wc -l`/`tail app.js` show it truncated mid-token (~7500 lines) while `grep` finds this session's real edits fine. NEW: `git status`/`git diff --stat` invented phantom changes to files never touched this session (`CLAUDE.md`, `MY_BOOKINGS_PHONE_OTP.md`, `homepage-preview.html`, `logo2.png`, `schema.md`, `test_postcss.mjs`) and a wildly wrong diff size (881 deletions vs 27 insertions — actual edits are net-positive by hundreds of lines). **Rule now extends to `git diff`, not just `git status`/file reads — never trust bash-computed repo state this session; cross-check against the user's own terminal.**

• **NOTHING COMMITTED/PUSHED.** Git commands handed to the user twice (after the DB+admin-panel work, and again after the 3 bug fixes) — not confirmed run either time. Do not commit without explicit go-ahead; if resuming, confirm with the user whether they ran the commands before assuming current git state. `node --check` via sandbox bash isn't trustworthy this session either (same mount issue) — user needs to run it on Windows before committing.

• **Not done**: no live browser/Chrome MCP check performed on localhost this session. Packages admin tab is edit-only for the existing 3 fixed tiers (no add/delete/reorder UI). Sticky-sidebar-overflow bug (from the 07-01 handoff below) still open, untouched.

## Session Handoff — 2026-07-02 (iCal sync bug: Ochre bookings blocked Umber — both edge fns fixed + DEPLOYED)

• **Bug: Umber(15) showed unavailable on dates only Ochre(16) was booked (Jul 2–9).** Two-part root cause, both in edge functions (app.js untouched — it correctly read rows that shouldn't exist). (1) **sync-ical Phase 1.5 fanout**: Airbnb's linked-listing feature blocks the Sienna(17) whole-floor listing whenever EITHER child is booked; those appear in Sienna's feed as `Airbnb (Not available)` events. sync-ical ignored SUMMARY and fanned ALL busy dates onto BOTH children as `source='parent', booking_id=NULL` — so an Ochre-only Airbnb booking blocked Umber. (2) **export-ical feedback loop**: export excluded `ical` rows ("loop prevention") but INCLUDED booking_id-NULL `parent` rows — i.e. Sienna's Airbnb data relabeled — so Airbnb's Umber listing imported the block, Umber's own feed then showed those dates busy, and the next sync wrote them back as Umber `ical` rows. Self-sustaining even after deleting the parent rows.

• **Fixes DEPLOYED LIVE (Supabase MCP, both v10; no local files exist in repo — deployed source is truth):** `sync-ical` now parses SUMMARY and propagates to children only `/reserved/i` events (`isReservation`); if a feed has zero SUMMARY lines it falls back to propagating everything (over-block beats double-book) and flags it in `last_ical_sync_status`. Status now reads e.g. `ok — 8 date(s), 0 reserved → 2 floor(s)`. `export-ical` now exports `parent` rows only with `booking_id NOT NULL` (own-venue query via `.or("source.eq.admin,and(source.eq.parent,booking_id.not.is.null)")`; combo-parent child query keeps child `admin`+`ical` but same parent filter).

• **Verified live**: triggered sync via the cron's own `net.http_post` + vault key (`ical_service_key`). Sienna status came back `0 reserved → 2 floor(s)` — confirming ALL 8 fanned dates were linked-listing echoes, zero genuine whole-floor bookings. `replace_parent_ical_blocks` with the now-empty date list auto-deleted every booking_id-NULL parent row on 15/16 (verified via SQL — no parent rows remain). Umber's export feed re-fetched clean (only old June admin blocks).

• **Remaining, self-healing**: Umber still has 10 `ical` rows (Jul 2–9 + 2027-07-01/02) because Airbnb's Umber calendar still holds the previously-imported blocks. Clears automatically once Airbnb refetches Umber's (now clean) export + next hourly sync runs; force it faster in Airbnb → Umber listing → Calendar → Connected calendars → refresh. ⚠ **2027-07-01/02 blocked on BOTH children's own feeds but NOT Sienna's** — doesn't fit the loop pattern; likely manual host blocks, user should eyeball in Airbnb.

• **Caveat to watch**: the SUMMARY-based filter trusts Airbnb's `Reserved` vs `Airbnb (Not available)` labeling. If Airbnb ever stops emitting SUMMARY, the fallback over-blocks (children get everything) — visible via the `(no SUMMARY in feed — propagated all)` suffix in the admin sync status.

