# The Picnic Stories — Claude Working Notes

## Session Handoff — 2026-07-02 (Phase 2 packages-first: plan doc + /packages Phase A + hero CTA built & browser-verified; sidebar bug fixed+committed — Phase A UNCOMMITTED)

• **Phase 2 plan** → `docs/PHASE2_PACKAGES_FIRST_PLAN.md` (plan-optimizer, 90/100), incl. **Phase 2.5** forward-compat for occasion-specific packages (date/movie night planned): nullable `packages.occasion` migration, augment-vs-replace decision deferred until packages defined, **required-addon rule** (silent add-on drop in `selectPackageTier` ~L2286 is fatal for themed packages — must hide package at venues that can't serve it, constrain From-price min + admin gap flags), admin add/delete UI becomes required, `OCCASIONS` key canon. Phase A + hero CTA IMPLEMENTED + verified live; **NEXT = Phase B** (homepage "Our Packages" section, 3 priced tier cards, hidden unless ≥1 enabled venue), then C (prerender `dist/packages.html` + sitemap 16 + meta/JSON-LD OfferCatalog + client price rehydration), then D (Meta Pixel ViewContent, mobile pass).

• **Handoff keystone (app.js)**: `appState.pendingPackage={occasion,tierKey,venueId}` + sessionStorage `ps_pending_pkg` (restored at bootstrap); `showVenuePage` clears it on venueId mismatch + prefills `bookingOccasion` from it; Book-Now click handler consumes it via the SAME `selectPackageTier()` the venue-first flow uses → tier step skipped, one code path, zero drift (parity verified live: identical locked bundle/prices both paths). Guest-step occasion dropdown: HIDDEN when `pending.occasion` set; shown w/ copy "Optional — helps us prep your setup" when occasion was skipped on /packages; original copy + behavior venue-first — all three branches verified live.

• **/packages page (app.js, inserted after `defaultTierForOccasion`)**: `pkgPageState`, `showPackagesPage(push,opts)` (awaits `loadPackages` via new `packagesLoaded` flag + `loadVenues`), `renderPackagesPage`, `renderPkgVenuePicker`, `pkgPageSelect{Occasion,Tier,Venue}`, `onPkgVenueGridClick` — all exposed on `window`. Deep links `?tier=&occasion=`; bootstrap `packagesPath` branch + popstate branch (`/^\/packages\/?$/`). `visiblePackagesFor(occasion)` filters `PACKAGE_TIER_ORDER` — used by BOTH /packages and `showPackageStep` (ladder copy chains only within universal packages). `packageTierPriceAt(v,tier,catalog,adults)` split from `packageTierPrice` (`PKG_PAGE_BASE_ADULTS=2` so stale `appState.adults` can't skew From-prices). `loadCatalogsForVenues(ids)` = ONE `add_ons.select('*, venue_add_ons!inner(venue_id)').in()` → `appState.venueCatalogCache` Map. `loadPackages` now carries `occasion: pkg.occasion || null` through.

• **User-directed refinements**: venue picker reuses `venueCardHtml(v,{priceHtml})` showing firm tier price — grid class must be `venue-grid` NOT `venues-grid` (that's the flex section stack; caught live as a 1-column bug); `.pkgp-venue-grid .venue-card-pkg-badge{display:none}`. Setting card lists `SHARED_SETUP_LABELS` (11 items, extracted const; `venueSetupLabels()` = shared + `metadata.includes`). Hero: **Contact Us replaced** by `#hero-packages-cta` "View Packages" (per user choice — floating WhatsApp covers contact), hidden until `revealPackagesEntryPoints()` (called from `loadVenues`) finds ≥1 `pkgEnabledVenues()`; SPA nav via inline onclick, real href for new-tab. Ghost style reads visually weak next to filled Book Now — user may want an outline variant.

• **Sticky sidebar overflow FIXED + committed by user** (style.css): `.vd-booking-card{max-height:calc(100vh - 104px);overflow-y:auto;overscroll-behavior:contain}` — cap on the CARD not `.vd-sidebar` so its box-shadow isn't clipped; reset (`max-height:none;overflow-y:visible`) in the ≤768px block + `.vd-layout--pkg-active`. Verified through calendar/slot/guest/package steps.

• **Data facts**: Beige(14) `base_price` changed 9,900→7,900 TODAY by user mid-session (Phase 0 setup-only pricing presumed; `metadata.tiers` now stale at 9,900 — dead data, flat path short-circuits it). User's browser holds sticky `ps_packages=1` QA override → all 6 cafes show in picker; production = Beige only (`packages_enabled`). Parked: venue 21 Skyshots insert (`venue_add_ons` venue 21 verified still missing addon 27; ₹4,000 elsewhere), Phase 0 pricing for remaining 5 cafes. Admin Packages tab verified working in browser (derived prices, per-venue table, venue-21 gap flag render correctly; earlier flat-pricing DB state confirmed via SQL).

• **UNCOMMITTED**: `app.js`, `index.html`, `style.css` (pkgp styles), `docs/PHASE2_PACKAGES_FIRST_PLAN.md`. Commit cmd handed over (`feat: packages-first /packages entry point (Phase 2A)…`), not confirmed run. Chrome MCP caveat: `Page.captureScreenshot` times out intermittently on this tab — verify via `javascript_tool` DOM reads instead.

## Session Handoff — 2026-07-02 (admin Packages panel built + shipped: DB migration, pricing model simplified, 3 venue-form display bugs fixed — UNCOMMITTED)

• **Goal: admin panel for package pricing + inclusions**, triggered by a base_price/`metadata.tiers` desync bug found while checking why package "From" prices looked stale. Ran plan-optimizer first (3 rounds, final score 89/100), then implemented directly — plan lives only in this conversation, not saved as a doc.

• **DB migration `add_pricing_columns_and_packages_tables` APPLIED LIVE**: `venues.free_guests_upto` (int) + promoted `overage_per_person` (was metadata-only) — populated ONLY for the 6 flattened cafe venues (14 Beige, 18 Sunroom, 19 Castle Valley, 20 Om Niwas, 21 Once Upon A Time, 24 House of Amer). `getVenuePrice()` now does flat `base_price` through `free_guests_upto` then linear overage for those; every other venue (partner_bnb 22/23/25 — genuine multi-step stay pricing; self_managed/custom/combo 5/15/16/17 — zero tiers) has `free_guests_upto=NULL` and falls through to the untouched legacy `metadata.tiers` path. **Caught by a parity SQL check before shipping**: first attempt set `free_guests_upto=capacity_max` for the zero-tier venues assuming it was lossless — wrong, it silently added overage charges past `capacity_max` that never existed before. Reverted to NULL; reran parity across guest counts 1–20 for every active venue → zero mismatches.

• **New `packages`/`package_add_ons` tables** replace the hardcoded `PACKAGE_TIERS` JS constant, seeded 1:1 from it (setting/moment/story, same add-on IDs) so nothing changed live on migration day. `PACKAGE_TIERS`/`PACKAGE_TIER_ORDER` in app.js are now `let`, overwritten by new `loadPackages()` at bootstrap (DOMContentLoaded, alongside `loadTeams()`) — hardcoded values are only the fallback if that fetch fails. RLS mirrors `admin_update_venues`; `get_advisors` shows only pre-existing warnings.

• **New admin "Packages" tab** (app.js `loadPackagesManager`/`renderPackagesManager`/`savePackageCard`): per-tier card (Setting/Moment/Story) — name/tagline/featured/add-ons checklist, price shown **read-only, always derived** (base_price + sum of checked add-on catalog prices — never typed in, specifically to not recreate the drift bug this feature started from). Per-venue price table flags add-on gaps — found venue 21 "Once Upon A Time" is missing add-on 27/Skyshots from its `venue_add_ons` catalog, so its Story tier has been silently underpriced/short one item this whole time — and shows food-included read from each venue's own `metadata.food_offline` (not editable per-package, single source of truth stays venue-level).

• **Venue form changes**: "Pricing Tiers" UI now gated `partner_bnb`-only (new `.vf-not-partner-only` class shows a plain `free_guests_upto` field for everyone else); added `#vf-food-offline` checkbox (cafe-only) wired to `metadata.food_offline` — previously only settable via direct SQL, invisible in admin.

• **Fixed 3 venue-form display bugs** (user-reported via screenshot; none were data loss — DB had correct values throughout, verified via direct query): (1) team select always reverting to the first team on reopen — `openVenueForm()` set `#vf-team`'s value in `populateVenueForm()` *before* rebuilding its `<option>` list right after, wiping the selection; reordered. (2) Packages checkbox always unchecked on reopen — `loadVenueManager()`'s explicit `.select()` was missing `packages_enabled` (and this session's own new `free_guests_upto`/`overage_per_person`); added all three. (3) No food-included indicator existed at all — added (see above).

• 🔴 **Sandbox bash mount unreliability confirmed again, now also hitting `git diff`** (not just app.js reads / `git status`, per prior entries): `wc -l`/`tail app.js` show it truncated mid-token (~7500 lines) while `grep` finds this session's real edits fine. NEW: `git status`/`git diff --stat` invented phantom changes to files never touched this session (`CLAUDE.md`, `MY_BOOKINGS_PHONE_OTP.md`, `homepage-preview.html`, `logo2.png`, `schema.md`, `test_postcss.mjs`) and a wildly wrong diff size (881 deletions vs 27 insertions — actual edits are net-positive by hundreds of lines). **Rule now extends to `git diff`, not just `git status`/file reads — never trust bash-computed repo state this session; cross-check against the user's own terminal.**

• **NOTHING COMMITTED/PUSHED.** Git commands handed to the user twice (after the DB+admin-panel work, and again after the 3 bug fixes) — not confirmed run either time. Do not commit without explicit go-ahead; if resuming, confirm with the user whether they ran the commands before assuming current git state. `node --check` via sandbox bash isn't trustworthy this session either (same mount issue) — user needs to run it on Windows before committing.

• **Not done**: no live browser/Chrome MCP check performed on localhost this session. Packages admin tab is edit-only for the existing 3 fixed tiers (no add/delete/reorder UI). Sticky-sidebar-overflow bug (from the 07-01 handoff below) still open, untouched.

## Session Handoff — 2026-07-02 (iCal sync bug: Ochre bookings blocked Umber — both edge fns fixed + DEPLOYED)

• **Bug: Umber(15) showed unavailable on dates only Ochre(16) was booked (Jul 2–9).** Two-part root cause, both in edge functions (app.js untouched — it correctly read rows that shouldn't exist). (1) **sync-ical Phase 1.5 fanout**: Airbnb's linked-listing feature blocks the Sienna(17) whole-floor listing whenever EITHER child is booked; those appear in Sienna's feed as `Airbnb (Not available)` events. sync-ical ignored SUMMARY and fanned ALL busy dates onto BOTH children as `source='parent', booking_id=NULL` — so an Ochre-only Airbnb booking blocked Umber. (2) **export-ical feedback loop**: export excluded `ical` rows ("loop prevention") but INCLUDED booking_id-NULL `parent` rows — i.e. Sienna's Airbnb data relabeled — so Airbnb's Umber listing imported the block, Umber's own feed then showed those dates busy, and the next sync wrote them back as Umber `ical` rows. Self-sustaining even after deleting the parent rows.

• **Fixes DEPLOYED LIVE (Supabase MCP, both v10; no local files exist in repo — deployed source is truth):** `sync-ical` now parses SUMMARY and propagates to children only `/reserved/i` events (`isReservation`); if a feed has zero SUMMARY lines it falls back to propagating everything (over-block beats double-book) and flags it in `last_ical_sync_status`. Status now reads e.g. `ok — 8 date(s), 0 reserved → 2 floor(s)`. `export-ical` now exports `parent` rows only with `booking_id NOT NULL` (own-venue query via `.or("source.eq.admin,and(source.eq.parent,booking_id.not.is.null)")`; combo-parent child query keeps child `admin`+`ical` but same parent filter).

• **Verified live**: triggered sync via the cron's own `net.http_post` + vault key (`ical_service_key`). Sienna status came back `0 reserved → 2 floor(s)` — confirming ALL 8 fanned dates were linked-listing echoes, zero genuine whole-floor bookings. `replace_parent_ical_blocks` with the now-empty date list auto-deleted every booking_id-NULL parent row on 15/16 (verified via SQL — no parent rows remain). Umber's export feed re-fetched clean (only old June admin blocks).

• **Remaining, self-healing**: Umber still has 10 `ical` rows (Jul 2–9 + 2027-07-01/02) because Airbnb's Umber calendar still holds the previously-imported blocks. Clears automatically once Airbnb refetches Umber's (now clean) export + next hourly sync runs; force it faster in Airbnb → Umber listing → Calendar → Connected calendars → refresh. ⚠ **2027-07-01/02 blocked on BOTH children's own feeds but NOT Sienna's** — doesn't fit the loop pattern; likely manual host blocks, user should eyeball in Airbnb.

• **Caveat to watch**: the SUMMARY-based filter trusts Airbnb's `Reserved` vs `Airbnb (Not available)` labeling. If Airbnb ever stops emitting SUMMARY, the fallback over-blocks (children get everything) — visible via the `(no SUMMARY in feed — propagated all)` suffix in the admin sync status.

## Session Handoff — 2026-07-01 (package step full-width/left-align bug fixed; Phase 1 packages confirmed pushed)

• **Phase 1 packages work is now committed + pushed** (the two 07-01 entries below this one) — confirmed via the user's own `git status` on Windows showing `app.js`/`admin.html` already clean. Only `CLAUDE.md` and `style.css` were outstanding this session; both committed with `git add CLAUDE.md style.css && git commit -m "fix: package step full-width layout on desktop..." && git push` (user confirmed pushed).

• **Real bug found + fixed: package step rendered at ~half viewport width, pinned left, on desktop.** Root cause was NOT a simple max-width cap (that was a red herring fixed first but insufficient) — `.vd-layout--pkg-active`'s `grid-template-areas` (style.css ~L9150) was missing a `"facts"` row, while `.vd-facts-outer` still carried the unconditional base rule `grid-area: facts`. With no "facts" area defined in the active template, the browser auto-placed that element into a **phantom implicit 3rd grid column**, which silently corrupted the whole grid's column sizing (confirmed via `getComputedStyle`: `grid-template-columns` read `"656px 0px 448px"` instead of one full-width track) — squeezing the "Choose your package" card into the first column and leaving the rest as dead space on the right.

• **Fix applied (style.css ~L9150-9176)**: added `"facts"` as its own row in `.vd-layout--pkg-active`'s `grid-template-areas`. Also (needed regardless, from the earlier width-cap investigation): `.vd-layout--pkg-active .vd-sidebar { width:100%; justify-self:stretch }`, `.vd-layout--pkg-active .vd-booking-card { max-width:none; width:100%; margin:0 auto; box-sizing:border-box }`, and `.vd-body.container:has(.vd-layout--pkg-active){ max-width:none }` to drop the outer `.container`'s 1280px cap specifically during the package step.

• **Verified live on the user's own `localhost:5173`** (not just production) — Chrome MCP can reach the user's local dev server directly (`http://localhost:5173/venues/beige-cafe?packages=1`), which is faster than round-tripping screenshots for future CSS/layout debugging. Confirmed via `getComputedStyle`/`getBoundingClientRect` before/after: grid went from 3 implicit columns to 1 true full-width column; the tier-card panel now spans full width and reads centered.

• 🔴 **Sandbox bash mount unreliability is broader than previously documented** — this session it didn't just truncate `app.js`/`style.css` contents, it showed a stale `git status` entirely: flagged `app.js`/`admin.html` as modified when the user's real Windows `git status` showed them already clean (committed/pushed in an earlier session). **Rule update: treat ALL bash-computed git state (status/diff/stat) in this sandbox as unreliable, not just file reads on app.js/style.css — cross-check important git decisions against the user's own terminal output before writing commit commands.**

• **Two things found but NOT fixed this session (still open):** (1) the venue-page "Check Availability" sticky sidebar is taller than the desktop viewport, so the time-slot picker / guest form / Book Now button often sit below the fold and require scrolling through most of the page's content to reach — a real discoverability bug. (2) Phase 0 flat tier-pricing (manual admin entry) still not done — tier cards still price off food-inflated per-guest stepping, not the target flat anchors (~₹9,900/₹13,500/₹26,000 @2 guests) noted in the 06-30 design-lock entry below.

## Session Handoff — 2026-07-01 (packages Phase 1 complete state + git-commands prep, torn-mount bug on app.js)

• **Full current state of Phase 1 packages feature** (all uncommitted, nothing pushed at the time): venue-first flow, cafe venues only — venue → calendar (date+slot) → guest step w/ occasion select → `showPackageStep` (3 priced tier cards: Setting/Moment/Story) → `showBookingForm` (locked bundle, included add-ons pre-checked + hidden via `.pkg-locked-check`). Toggle is now `venues.packages_enabled` DB column (admin-editable, cafe-only checkbox in `admin.html`) with `?packages=1/0` as a sticky QA override on top — replaces the old global `PACKAGES_FLAG_DEFAULT` flag entirely. **Since pushed — see entry above.**

• **Load-bearing impl notes:** `appState.selectedPackage={occasion,tierKey,addonIds}` + `appState.bookingOccasion`, reset in `showVenuePage`. `appState` is NOT on `window` (ES module) — inline `onclick` handlers must call exposed `window.*` fns (`setBookingOccasion`, `selectPackageTier`, `showPackageBack`, `changePackage`, `changePackageFromIntent`). Tier card price = client-side `getPicnicPrice(adults) + Σ tier add-on catalog prices` (not RPC). `packageFlowActive(venue)` is the single gate everything should call.

• **Phase 0 pricing (manual admin task) still NOT applied** — tier cards still use food-inflated per-guest stepping; `food_offline:true` is live in DB on venues 14,18,19,20,21,24 but base tiers are unchanged, so cards will self-correct once flat setup-only tier pricing is re-entered in admin. `getInclusions()` already hides the inclusion banner reversibly when `food_offline` is set; `handleVenueFormSubmit` metadata write was fixed to MERGE (not destructively replace) so admin saves preserve `food_multiplier`/`food_offline`.

• 🔴 **Sandbox bash mount serves STALE/TRUNCATED reads of `app.js`** (confirmed this session) — `git diff`/`wc -l` via bash showed app.js missing its last 249 lines (entire `DOMContentLoaded` bootstrap gone). Verified via the Read tool that the real file is intact (7829 lines). Same recurring issue previously seen on `style.css` and an edge function. **See the broader rule update in the entry above — this now extends to git status/diff output, not just file reads.**

• **CONTINUE FROM (superseded — see entry above):** Phase 0 manual pricing entry in admin, homepage packages-first entry point (deferred), admin loud-flag for gated add-ons 29/30/32 (deferred), fix the "Check Availability" sticky-sidebar overflow bug.

## Session Handoff — 2026-07-01 (packages: per-venue DB toggle replaces session flag)

• **Discoverability gap closed properly.** After a first pass added a venue-card badge, user corrected the actual ask: a real `venues.packages_enabled` DB column, admin-editable, as the production source of truth — not a URL/session flag. Implemented as directed; `?packages=1|0` kept as a QA override layered on top (per user's explicit choice), not removed.

• **DB migration `add_packages_enabled_to_venues` APPLIED LIVE**: `venues.packages_enabled boolean NOT NULL DEFAULT false`. RLS checked first (`admin_update_venues` is row-level only, gated by `auth.email()='aksh.eeev@gmail.com'`, no column restrictions) — no policy changes needed. `get_advisors` (security) run post-migration: zero new findings, all warnings pre-existing.

• **`app.js` flag mechanism replaced**: old `packagesEnabled()`/`PACKAGES_FLAG_DEFAULT` removed entirely (verified via grep, no remaining refs). New `packagesOverride()` reads `?packages=1|0` → persists sticky `localStorage['ps_packages']`, returns `true`/`false`/`null`. New `packageFlowActive(venue)` = `venue.type==='cafe'` gate → override if set → else `!!venue.packages_enabled`. All call sites updated (click-delegation routing handler, venue card badge, etc.) to call `packageFlowActive(venue)` instead of the old global check.

• **Admin form wired end-to-end**: `admin.html` — new `.vf-toggle-label.vf-cafe-only` checkbox `#vf-packages-enabled` next to the existing Active/Requires-confirmation toggles (hidden by default, same pattern as `.vf-partner-only`). `app.js`: `updateVfTypeVisibility()` now toggles `.vf-cafe-only` (visible only when `type==='cafe'`); `clearVenueForm()` resets it to unchecked; `populateVenueForm()` sets it from `!!venue.packages_enabled`; `handleVenueFormSubmit()` payload adds `packages_enabled` as a **top-level column** (not inside `metadata`, per user's explicit choice over the alternative of a metadata key).

• **`loadVenues()` confirmed** already `select('*')` — new column reaches the storefront/`packageFlowActive()` with no further changes needed.

## Session Handoff — 2026-06-23 (admin query pipeline: status + filter/sort + edit modal)

• **Goal**: tame the cluttered admin queries list + stop leads getting lost. Shipped admin-side lead pipeline. Customer self-confirm/self-edit was explicitly CUT (out of scope, all the risk lived there). DB migration LIVE; `app.js`/`admin.html`/`style.css` LOCAL, uncommitted.

• **DB migration `add_query_status_to_bookings` APPLIED LIVE**: `bookings.query_status text NOT NULL DEFAULT 'new'` + check constraint `IN ('new','in_talk','quoted','no_reply','lost')`. NOT-NULL-default backfilled all existing rows to `new` (4 unconfirmed + 1 confirmed). Status is ignored once `confirmed=true` (those move to bookings tab). Sort order top→bottom = new, in_talk, quoted, no_reply, lost. Status set chosen by user over a 6th "cost issue" value (kept stage-based; cost-issue is a reason, belongs in notes).

• **`app.js` (~line 3265, before `renderQueries`)**: new block — `QUERY_STATUSES`/`QUERY_STATUS_META`/`QUERY_STATUS_ORDER`, `adminQueryStatusFilter` state, `setQueryStatusFilter`, `normalizedQueryStatus`, `queryStatusSelectHtml` (inline `<select onchange=updateQueryStatus>`), `updateQueryStatus` (PATCH bookings.query_status via admin client), and the edit modal: `openQueryEdit`/`closeQueryEdit`/`saveQueryEdit` + module var `queryEditState`. `renderQueries` body now: team filter → status filter → sort by status priority then created_at desc; empty-state is filter-aware. Card header: hardcoded New dot/badge REPLACED with `queryStatusSelectHtml`. Footer: new `.adm-edit-btn`. All new fns exposed via `window.fn = fn` (module, like `setAdminTeamFilter`).

• **Edit modal** (self-contained overlay injected into `document.body`, removed on close — not a `.modal`/`.hidden` element): edits full_name, mobile (10-digit guard), email, guest_count, children_count, preferred_date, checkout_date (stays only), time_slot (cafe only, from `CAFE_SLOTS`), occasion (preset dropdown mirroring storefront + free-text "Other" fallback), celebration board (type select black/white + message → jsonb `{type,message}`, same shape as `submitBookingIntent`), special_requirements → single UPDATE on bookings. Add-ons: loads `loadVenueAddOns(venue_id)` catalog ∪ currently-attached, checkbox diff → delete removed `booking_add_ons` rows by id + insert added (price_at_booking/name/requires_confirmation from catalog). Does NOT touch `confirmed` or recompute advance (admin still types advance at confirm). RLS verified: `admin_update_bookings` + `admin_all_booking_add_ons` cover all writes.

• **Query card pricing**: header-right shows auto-computed `total_amount` + suggested 50% `advance_amount` as `.adm-price-badge--total`/`--adv` (0 → hidden for combo/partner/custom leads). Shown on ALL screens (was briefly desktop-only; user wanted it everywhere — mobile just shrinks the badge). Query card header wrapped in `.adm-card-header-right` (mirrors bookings card). Admin still types the real advance in the footer input at confirm.

• **Edit modal RE-COMPUTES pricing on save**: after the add-on diff, `saveQueryEdit` calls `supabase.rpc('compute_booking_total', {p_venue_id, p_billing_guests, p_nights, p_addon_ids, p_time_slot})` then writes `total_amount` + `advance_amount = round(total*0.5)` in the same bookings UPDATE — so editing guests/dates/slot/add-ons keeps the card price in sync (this fixed the "add-on change didn't update total/advance" bug). Param mapping MIRRORS `submit_booking_intent`: billing guests = `guests − children`, nights = `checkout − preferred` (0 if no checkout), addon_ids = final checked set. `compute_booking_total` is EXECUTE-granted to authenticated (verified); `compute_booking_advance` is NOT (don't call it — compute advance in JS). Verified live: RPC reproduces stored totals exactly for unedited bookings (ids 4,7).

• **`admin.html`**: added `.adm-status-filters` pill row (All/New/In talk/Quoted/No reply/Lost) under the team pills in the queries tab only. **`style.css`** (after `.adm-team-pill.active`): `.adm-status-filters`/`-pill`, `.adm-status-select` + `.adm-qs--*` color variants, `.adm-edit-btn`, full `.qedit-*` modal styles; `.adm-price-badge--total/--adv` (after `.adm-amount-badge`).

• **Verify done**: `node --check` clean; no smart-quote delimiters (the recurring crash); live-data check confirms cafe/custom/stay field branches. Run `npm run build` on Windows before deploy (sandbox esbuild crashes / torn mounts).

## Session Handoff — 2026-06-23 (SEO city pages + FOUC fix)

• **City landing pages IMPLEMENTED** — `/picnic-venues-jaipur` and `/picnic-venues-gurugram` generated by `scripts/prerender-venues.mjs` via `buildCityPages()` + `CITY_CONFIG`. Not static HTML — prerender script fetches live Supabase data and writes to `dist/` after Vite build.

• **CSS class collision fix** — site's compiled `style.css` defines `.hero`, `.venue-card`, `.venue-section` for the SPA; city pages loading the same CSS inherited those styles, causing the hero to appear as a ~80vh empty block on mobile. Fix: all city page classes renamed with `city-` prefix (`.city-hero`, `.city-card`, `.city-section`, `.city-section--alt`, `.city-breadcrumb`, `.city-jump-btn`).

• **FOUC fix — loading overlay on prerendered venue pages** — navigating from a city page to `/venues/<slug>` = full browser navigation; raw SEO `<h1>/<p>` content showed for 1–2s while 525KB app.js downloaded. Fix: `buildPage()` in prerender injects `#pr-loader` overlay (logo + progress bar). MutationObserver watches for first `[class*="vd-"]` element → fades out in 280ms. Fallbacks: `window.load+300ms`, 6s hard cap.

• **Localhost vs prod caveat** — `npm run dev` serves SPA with history fallback so city page URLs 404 locally. Only testable via `npm run build && npm run preview`. City pages exist only in `dist/` post-build. Sitemap now emits 15 URLs (1 home + 12 venue + 2 city).

## Session Handoff — 2026-06-29 (packages spec — SPEC_occasion_packages.md created, superseded by 06-30 design lock)

• **`docs/SPEC_occasion_packages.md` created** — original two-part spec (homepage package tiers + occasion-based add-on pre-fill). Key insight from historical data: ₹180k+ of extras sold offline last year — this is a capture problem, not a demand problem. **Flat pricing in this spec (₹8,900/12,900/24,900) was later found fictional and superseded — see 06-30 entry.**

## Session Handoff — 2026-06-30 (packages feature — design locked: universal tiers + venue-page pricing)

• **Goal = raise AOV.** Decided: packages are a **front door on the existing booking engine**, NOT a pipeline replacement (rejected ditching the pipeline — would discard availability logic, Razorpay+webhook, SEO `/venues/<slug>` pages, admin tooling to solve a curation/attach problem). Top AOV lever = defaulting **Photographer (id 19, ₹6,000)** ON for proposals (sold 11× offline, ~never online, 4× next add-on; proposals = 28% of bookings).

• **Pricing RESOLVED — no price matrix.** A tier = a curated add-on preset; per-combo price = existing `compute_booking_total(p_venue_id,p_billing_guests,p_nights,p_addon_ids,p_time_slot)` which already returns a unique price per venue+add-on set. "From ₹X" = min across venues at base guests. Live cafe/self_managed bases: Beige(14) 9900, Sunroom(18) 13900, Om Niwas(20) 12900, Once Upon A Time(21) 15900, TerraCottage Umber(15) 10900/Ochre(16) 7900.

• **Tier model RESOLVED → fixed universal `Setting/Moment/Story`** (NOT bespoke per-occasion). Sets: `setting:[]`, `moment:[22,24,17]`, `story:[19,27,23,22,24,17]`. "From": Setting ₹9,900 · Moment ~₹13,500 · Story ~₹26,000 (@2 guests). Occasion demoted to a **default-biasing nudge** (Proposal→default Story/ensure Photographer; Birthday→default Moment), not a tier system. All universal-tier IDs verified `requires_confirmation=false`; never pre-fill 29/30/32.

• **Two converging paths (Phase 2, deferred).** Home: "Our Venues" + "Our Packages". Packages-first: occasion → tier(from ₹X) → venue(price firms) → form. Venue-first (Phase 1, built): venue page shows 3 tiers w/ this-venue prices → tier → form. **Keystone:** `appState.selectedPackage = {occasion,tierKey,addonIds}`; booking form reads ONLY this; both paths must write it (prevents drift).
